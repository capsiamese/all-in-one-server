// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"log"

	"aio/ent/migrate"

	"aio/ent/extensionclient"
	"aio/ent/group"
	"aio/ent/tab"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
)

// Client is the client that holds all ent builders.
type Client struct {
	config
	// Schema is the client for creating, migrating and dropping schema.
	Schema *migrate.Schema
	// ExtensionClient is the client for interacting with the ExtensionClient builders.
	ExtensionClient *ExtensionClientClient
	// Group is the client for interacting with the Group builders.
	Group *GroupClient
	// Tab is the client for interacting with the Tab builders.
	Tab *TabClient
}

// NewClient creates a new client configured with the given options.
func NewClient(opts ...Option) *Client {
	cfg := config{log: log.Println, hooks: &hooks{}}
	cfg.options(opts...)
	client := &Client{config: cfg}
	client.init()
	return client
}

func (c *Client) init() {
	c.Schema = migrate.NewSchema(c.driver)
	c.ExtensionClient = NewExtensionClientClient(c.config)
	c.Group = NewGroupClient(c.config)
	c.Tab = NewTabClient(c.config)
}

// Open opens a database/sql.DB specified by the driver name and
// the data source name, and returns a new client attached to it.
// Optional parameters can be added for configuring the client.
func Open(driverName, dataSourceName string, options ...Option) (*Client, error) {
	switch driverName {
	case dialect.MySQL, dialect.Postgres, dialect.SQLite:
		drv, err := sql.Open(driverName, dataSourceName)
		if err != nil {
			return nil, err
		}
		return NewClient(append(options, Driver(drv))...), nil
	default:
		return nil, fmt.Errorf("unsupported driver: %q", driverName)
	}
}

// Tx returns a new transactional client. The provided context
// is used until the transaction is committed or rolled back.
func (c *Client) Tx(ctx context.Context) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, fmt.Errorf("ent: cannot start a transaction within a transaction")
	}
	tx, err := newTx(ctx, c.driver)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = tx
	return &Tx{
		ctx:             ctx,
		config:          cfg,
		ExtensionClient: NewExtensionClientClient(cfg),
		Group:           NewGroupClient(cfg),
		Tab:             NewTabClient(cfg),
	}, nil
}

// BeginTx returns a transactional client with specified options.
func (c *Client) BeginTx(ctx context.Context, opts *sql.TxOptions) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, fmt.Errorf("ent: cannot start a transaction within a transaction")
	}
	tx, err := c.driver.(interface {
		BeginTx(context.Context, *sql.TxOptions) (dialect.Tx, error)
	}).BeginTx(ctx, opts)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = &txDriver{tx: tx, drv: c.driver}
	return &Tx{
		ctx:             ctx,
		config:          cfg,
		ExtensionClient: NewExtensionClientClient(cfg),
		Group:           NewGroupClient(cfg),
		Tab:             NewTabClient(cfg),
	}, nil
}

// Debug returns a new debug-client. It's used to get verbose logging on specific operations.
//
//	client.Debug().
//		ExtensionClient.
//		Query().
//		Count(ctx)
//
func (c *Client) Debug() *Client {
	if c.debug {
		return c
	}
	cfg := c.config
	cfg.driver = dialect.Debug(c.driver, c.log)
	client := &Client{config: cfg}
	client.init()
	return client
}

// Close closes the database connection and prevents new queries from starting.
func (c *Client) Close() error {
	return c.driver.Close()
}

// Use adds the mutation hooks to all the entity clients.
// In order to add hooks to a specific client, call: `client.Node.Use(...)`.
func (c *Client) Use(hooks ...Hook) {
	c.ExtensionClient.Use(hooks...)
	c.Group.Use(hooks...)
	c.Tab.Use(hooks...)
}

// ExtensionClientClient is a client for the ExtensionClient schema.
type ExtensionClientClient struct {
	config
}

// NewExtensionClientClient returns a client for the ExtensionClient from the given config.
func NewExtensionClientClient(c config) *ExtensionClientClient {
	return &ExtensionClientClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `extensionclient.Hooks(f(g(h())))`.
func (c *ExtensionClientClient) Use(hooks ...Hook) {
	c.hooks.ExtensionClient = append(c.hooks.ExtensionClient, hooks...)
}

// Create returns a create builder for ExtensionClient.
func (c *ExtensionClientClient) Create() *ExtensionClientCreate {
	mutation := newExtensionClientMutation(c.config, OpCreate)
	return &ExtensionClientCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of ExtensionClient entities.
func (c *ExtensionClientClient) CreateBulk(builders ...*ExtensionClientCreate) *ExtensionClientCreateBulk {
	return &ExtensionClientCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for ExtensionClient.
func (c *ExtensionClientClient) Update() *ExtensionClientUpdate {
	mutation := newExtensionClientMutation(c.config, OpUpdate)
	return &ExtensionClientUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *ExtensionClientClient) UpdateOne(ec *ExtensionClient) *ExtensionClientUpdateOne {
	mutation := newExtensionClientMutation(c.config, OpUpdateOne, withExtensionClient(ec))
	return &ExtensionClientUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *ExtensionClientClient) UpdateOneID(id int) *ExtensionClientUpdateOne {
	mutation := newExtensionClientMutation(c.config, OpUpdateOne, withExtensionClientID(id))
	return &ExtensionClientUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for ExtensionClient.
func (c *ExtensionClientClient) Delete() *ExtensionClientDelete {
	mutation := newExtensionClientMutation(c.config, OpDelete)
	return &ExtensionClientDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a delete builder for the given entity.
func (c *ExtensionClientClient) DeleteOne(ec *ExtensionClient) *ExtensionClientDeleteOne {
	return c.DeleteOneID(ec.ID)
}

// DeleteOneID returns a delete builder for the given id.
func (c *ExtensionClientClient) DeleteOneID(id int) *ExtensionClientDeleteOne {
	builder := c.Delete().Where(extensionclient.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &ExtensionClientDeleteOne{builder}
}

// Query returns a query builder for ExtensionClient.
func (c *ExtensionClientClient) Query() *ExtensionClientQuery {
	return &ExtensionClientQuery{
		config: c.config,
	}
}

// Get returns a ExtensionClient entity by its id.
func (c *ExtensionClientClient) Get(ctx context.Context, id int) (*ExtensionClient, error) {
	return c.Query().Where(extensionclient.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *ExtensionClientClient) GetX(ctx context.Context, id int) *ExtensionClient {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryGroups queries the groups edge of a ExtensionClient.
func (c *ExtensionClientClient) QueryGroups(ec *ExtensionClient) *GroupQuery {
	query := &GroupQuery{config: c.config}
	query.path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := ec.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(extensionclient.Table, extensionclient.FieldID, id),
			sqlgraph.To(group.Table, group.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, extensionclient.GroupsTable, extensionclient.GroupsColumn),
		)
		fromV = sqlgraph.Neighbors(ec.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *ExtensionClientClient) Hooks() []Hook {
	return c.hooks.ExtensionClient
}

// GroupClient is a client for the Group schema.
type GroupClient struct {
	config
}

// NewGroupClient returns a client for the Group from the given config.
func NewGroupClient(c config) *GroupClient {
	return &GroupClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `group.Hooks(f(g(h())))`.
func (c *GroupClient) Use(hooks ...Hook) {
	c.hooks.Group = append(c.hooks.Group, hooks...)
}

// Create returns a create builder for Group.
func (c *GroupClient) Create() *GroupCreate {
	mutation := newGroupMutation(c.config, OpCreate)
	return &GroupCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Group entities.
func (c *GroupClient) CreateBulk(builders ...*GroupCreate) *GroupCreateBulk {
	return &GroupCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Group.
func (c *GroupClient) Update() *GroupUpdate {
	mutation := newGroupMutation(c.config, OpUpdate)
	return &GroupUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *GroupClient) UpdateOne(gr *Group) *GroupUpdateOne {
	mutation := newGroupMutation(c.config, OpUpdateOne, withGroup(gr))
	return &GroupUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *GroupClient) UpdateOneID(id int) *GroupUpdateOne {
	mutation := newGroupMutation(c.config, OpUpdateOne, withGroupID(id))
	return &GroupUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Group.
func (c *GroupClient) Delete() *GroupDelete {
	mutation := newGroupMutation(c.config, OpDelete)
	return &GroupDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a delete builder for the given entity.
func (c *GroupClient) DeleteOne(gr *Group) *GroupDeleteOne {
	return c.DeleteOneID(gr.ID)
}

// DeleteOneID returns a delete builder for the given id.
func (c *GroupClient) DeleteOneID(id int) *GroupDeleteOne {
	builder := c.Delete().Where(group.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &GroupDeleteOne{builder}
}

// Query returns a query builder for Group.
func (c *GroupClient) Query() *GroupQuery {
	return &GroupQuery{
		config: c.config,
	}
}

// Get returns a Group entity by its id.
func (c *GroupClient) Get(ctx context.Context, id int) (*Group, error) {
	return c.Query().Where(group.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *GroupClient) GetX(ctx context.Context, id int) *Group {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryTabs queries the tabs edge of a Group.
func (c *GroupClient) QueryTabs(gr *Group) *TabQuery {
	query := &TabQuery{config: c.config}
	query.path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := gr.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(group.Table, group.FieldID, id),
			sqlgraph.To(tab.Table, tab.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, group.TabsTable, group.TabsColumn),
		)
		fromV = sqlgraph.Neighbors(gr.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryClient queries the client edge of a Group.
func (c *GroupClient) QueryClient(gr *Group) *ExtensionClientQuery {
	query := &ExtensionClientQuery{config: c.config}
	query.path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := gr.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(group.Table, group.FieldID, id),
			sqlgraph.To(extensionclient.Table, extensionclient.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, group.ClientTable, group.ClientColumn),
		)
		fromV = sqlgraph.Neighbors(gr.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *GroupClient) Hooks() []Hook {
	return c.hooks.Group
}

// TabClient is a client for the Tab schema.
type TabClient struct {
	config
}

// NewTabClient returns a client for the Tab from the given config.
func NewTabClient(c config) *TabClient {
	return &TabClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `tab.Hooks(f(g(h())))`.
func (c *TabClient) Use(hooks ...Hook) {
	c.hooks.Tab = append(c.hooks.Tab, hooks...)
}

// Create returns a create builder for Tab.
func (c *TabClient) Create() *TabCreate {
	mutation := newTabMutation(c.config, OpCreate)
	return &TabCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Tab entities.
func (c *TabClient) CreateBulk(builders ...*TabCreate) *TabCreateBulk {
	return &TabCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Tab.
func (c *TabClient) Update() *TabUpdate {
	mutation := newTabMutation(c.config, OpUpdate)
	return &TabUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *TabClient) UpdateOne(t *Tab) *TabUpdateOne {
	mutation := newTabMutation(c.config, OpUpdateOne, withTab(t))
	return &TabUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *TabClient) UpdateOneID(id int) *TabUpdateOne {
	mutation := newTabMutation(c.config, OpUpdateOne, withTabID(id))
	return &TabUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Tab.
func (c *TabClient) Delete() *TabDelete {
	mutation := newTabMutation(c.config, OpDelete)
	return &TabDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a delete builder for the given entity.
func (c *TabClient) DeleteOne(t *Tab) *TabDeleteOne {
	return c.DeleteOneID(t.ID)
}

// DeleteOneID returns a delete builder for the given id.
func (c *TabClient) DeleteOneID(id int) *TabDeleteOne {
	builder := c.Delete().Where(tab.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &TabDeleteOne{builder}
}

// Query returns a query builder for Tab.
func (c *TabClient) Query() *TabQuery {
	return &TabQuery{
		config: c.config,
	}
}

// Get returns a Tab entity by its id.
func (c *TabClient) Get(ctx context.Context, id int) (*Tab, error) {
	return c.Query().Where(tab.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *TabClient) GetX(ctx context.Context, id int) *Tab {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryGroup queries the group edge of a Tab.
func (c *TabClient) QueryGroup(t *Tab) *GroupQuery {
	query := &GroupQuery{config: c.config}
	query.path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := t.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(tab.Table, tab.FieldID, id),
			sqlgraph.To(group.Table, group.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, tab.GroupTable, tab.GroupColumn),
		)
		fromV = sqlgraph.Neighbors(t.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *TabClient) Hooks() []Hook {
	return c.hooks.Tab
}
